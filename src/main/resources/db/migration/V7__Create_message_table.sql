CREATE TABLE message (
    m_id INTEGER GENERATED BY DEFAULT AS IDENTITY,
    m_name VARCHAR(255),
    m_email VARCHAR(255),
    m_msg VARCHAR(510),
    m_date TIMESTAMP,
    m_read BOOLEAN,
    PRIMARY KEY (m_id)
);